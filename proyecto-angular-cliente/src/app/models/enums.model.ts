export enum Action {
    Aniadir = 0,
    Modificar = 1,
    Eliminar = 2,
    Activar = 3
}

export enum Rol {
    Desarrollador = "Desarrollador",
    Otro = "Otro"
}

export enum Status
{
    Success = 0,
    NotFound = 1,
    Error = 2,
}
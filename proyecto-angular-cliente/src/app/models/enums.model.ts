export enum Action {
    Añadir = 0,
    Modificar = 1,
    Eliminar = 2,
    Activar = 3
}

export enum Rol {
    Desarrollador = 0,
    Otro = 1
}

export enum Status
{
    Success = 0,
    NotFound = 1,
    Error = 2,
}